import { StyleSheet } from "react-native";

const global = StyleSheet.create({
    text: {
        fontSize: 20,
        fontWeight: "bold",
        textAlign: "center",
    }
})

export default global